<%= form_with(model: participante) do |form| %>
  <% if participante.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(participante.errors.count, "error") %> prohibited this participante from being saved:</h2>

      <ul>
        <% participante.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :app_paterno %>
    <%= form.text_field :app_paterno %>
  </div>

  <div class="field">
    <%= form.label :app_materno %>
    <%= form.text_field :app_materno %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :evento_id %>
    <%= form.text_field :evento_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
